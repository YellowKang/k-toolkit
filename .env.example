# 服务端口（默认 3000）
PORT=3000

# JWT 签名密钥（必填，生产环境请使用足够长的随机字符串）
# 生成示例：node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-secret-key-here

# 允许跨域的前端地址（多个用逗号分隔，留空则只允许同源）
# 开发环境示例：http://localhost:5173,http://localhost:3000
CORS_ORIGIN=
